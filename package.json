{
  "name": "latin-academy",
  "version": "0.9.0",
  "private": true,
  "description": "Static family learning website for Latin.",
  "scripts": {
    "check:syntax": "node --check static/app.js && node --check static/profiles.js && node --check static/profiles-page.js && node --check static/introduction.js && node --check static/foundations.js",
    "check:site": "node scripts/validate-site.mjs",
    "test": "npm run check:syntax && npm run check:site"
  }
}
